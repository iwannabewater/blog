---
import IconX from "@/assets/icons/IconX.svg";
import IconMoon from "@/assets/icons/IconMoon.svg";
import IconSearch from "@/assets/icons/IconSearch.svg";
import IconArchive from "@/assets/icons/IconArchive.svg";
import IconSunHigh from "@/assets/icons/IconSunHigh.svg";
import IconMenuDeep from "@/assets/icons/IconMenuDeep.svg";
import IconCloudWoodMark from "@/assets/icons/IconCloudWoodMark.svg";
import IconThemeSystem from "@/assets/icons/IconThemeSystem.svg";
import LinkButton from "./LinkButton.astro";
import { SITE } from "@/config";
import { withBase, withoutBase } from "@/utils/basePath";

const { pathname } = Astro.url;

// Remove trailing slash from current pathname if exists
const currentPath =
  withoutBase(pathname).endsWith("/") && withoutBase(pathname) !== "/"
    ? withoutBase(pathname).slice(0, -1)
    : withoutBase(pathname);

const isActive = (path: string) => {
  const currentPathArray = currentPath.split("/").filter(p => p.trim());
  const pathArray = path.split("/").filter(p => p.trim());

  return currentPath === path || currentPathArray[0] === pathArray[0];
};
---

<a
  id="skip-to-content"
  href="#main-content"
  class="absolute start-16 -top-full z-50 rounded-md border border-border bg-surface px-3 py-2 font-ui text-sm text-accent shadow-sm backdrop-blur-lg transition-all focus:top-4"
>
  Skip to content
</a>

<header
  class="sticky top-0 z-30 border-b border-border/75 bg-background/90 backdrop-blur-xl"
>
  <div class="app-layout">
    <div
      id="top-nav-wrap"
      class="relative flex min-h-18 w-full items-center justify-between py-3"
    >
      <a
        href={withBase("/")}
        class="group flex min-w-0 items-center gap-3 py-1 no-underline"
      >
        <span
          aria-hidden="true"
          class="grid size-11 shrink-0 place-items-center rounded-lg border border-border bg-surface text-accent shadow-sm transition-colors group-hover:border-accent/60 group-hover:text-accent-secondary"
        >
          <IconCloudWoodMark class="size-8" />
        </span>
        <span class="min-w-0">
          <span
            class="block font-heading text-2xl leading-none font-medium sm:text-3xl"
          >
            {SITE.title}
          </span>
          <span
            class="mt-1 hidden font-ui text-base leading-none text-muted-foreground sm:block"
          >
            桨声 / 灯影 / 木栖云
          </span>
        </span>
      </a>
      <nav id="nav-menu" class="flex items-center justify-end font-ui">
        <button
          id="menu-btn"
          class="focus-outline grid size-10 place-items-center rounded-lg border border-border bg-surface sm:hidden"
          aria-label="打开菜单"
          aria-expanded="false"
          aria-controls="menu-items"
        >
          <IconX id="close-icon" class="hidden" />
          <IconMenuDeep id="menu-icon" />
        </button>
        <ul
          id="menu-items"
          class:list={[
            "absolute end-0 top-14 grid w-64 grid-cols-2 gap-2 rounded-lg border border-border bg-surface p-2 shadow-lg",
            "[&>li>a]:block [&>li>a]:rounded-md [&>li>a]:px-3 [&>li>a]:py-2.5 [&>li>a]:text-center [&>li>a]:text-base [&>li>a]:font-medium [&>li>a]:no-underline [&>li>a]:hover:bg-muted [&>li>a]:hover:text-accent",
            "hidden",
            "sm:static sm:flex sm:w-auto sm:grid-cols-none sm:items-center sm:gap-1 sm:border-0 sm:bg-transparent sm:p-0 sm:shadow-none sm:[&>li>a]:flex sm:[&>li>a]:h-10 sm:[&>li>a]:items-center sm:[&>li>a]:px-3 sm:[&>li>a]:py-0 sm:[&>li>a]:text-lg",
          ]}
        >
          <li class="col-span-2">
            <a
              href={withBase("/posts")}
              class:list={{ "active-nav": isActive("/posts") }}
            >
              文章
            </a>
          </li>
          <li class="col-span-2">
            <a
              href={withBase("/tags")}
              class:list={{ "active-nav": isActive("/tags") }}
            >
              标签
            </a>
          </li>
          <li class="col-span-2">
            <a
              href={withBase("/about")}
              class:list={{ "active-nav": isActive("/about") }}
            >
              关于
            </a>
          </li>
          {
            SITE.showArchives && (
              <li class="col-span-2">
                <LinkButton
                  href={withBase("/archives")}
                  class:list={[
                    "focus-outline flex justify-center rounded-md p-3 sm:size-10 sm:items-center sm:p-0 [&>svg]:size-6",
                    {
                      "active-nav [&>svg]:stroke-accent": isActive("/archives"),
                    },
                  ]}
                  title="归档"
                  aria-label="归档"
                >
                  <IconArchive class="hidden sm:inline-block" />
                  <span class="sm:sr-only">归档</span>
                </LinkButton>
              </li>
            )
          }
          <li class="col-span-1 flex items-center justify-center">
            <LinkButton
              href={withBase("/search")}
              class:list={[
                "focus-outline flex rounded-md p-3 sm:size-10 sm:items-center sm:justify-center sm:p-0 [&>svg]:size-6",
                { "[&>svg]:stroke-accent": isActive("/search") },
              ]}
              title="搜索"
              aria-label="搜索"
            >
              <IconSearch />
              <span class="sr-only">搜索</span>
            </LinkButton>
          </li>
          {
            SITE.lightAndDarkMode && (
              <li class="col-span-1 flex items-center justify-center">
                <button
                  id="theme-btn"
                  class="focus-outline relative size-12 rounded-md p-3 hover:bg-muted sm:size-10 sm:p-2 hover:[&>svg]:stroke-accent"
                  title="当前主题：跟随系统，点击切换"
                  aria-label="当前主题：跟随系统"
                  aria-live="polite"
                  data-theme-mode="system"
                >
                  <IconThemeSystem class="theme-icon theme-icon-system" />
                  <IconSunHigh class="theme-icon theme-icon-light" />
                  <IconMoon class="theme-icon theme-icon-dark" />
                </button>
              </li>
            )
          }
        </ul>
      </nav>
    </div>
  </div>
</header>

<script>
  function toggleNav() {
    const menuBtn = document.querySelector("#menu-btn");
    const menuItems = document.querySelector("#menu-items");
    const menuIcon = document.querySelector("#menu-icon");
    const closeIcon = document.querySelector("#close-icon");

    if (!menuBtn || !menuItems || !menuIcon || !closeIcon) return;

    menuBtn.addEventListener("click", () => {
      const openMenu = menuBtn.getAttribute("aria-expanded") === "true";

      menuBtn.setAttribute("aria-expanded", openMenu ? "false" : "true");
      menuBtn.setAttribute("aria-label", openMenu ? "打开菜单" : "关闭菜单");

      menuItems.classList.toggle("hidden");
      menuIcon.classList.toggle("hidden");
      closeIcon.classList.toggle("hidden");
    });
  }

  toggleNav();

  // Runs on view transitions navigation
  document.addEventListener("astro:after-swap", toggleNav);
</script>
