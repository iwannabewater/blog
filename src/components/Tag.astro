---
import IconHash from "@/assets/icons/IconHash.svg";
import { withBase } from "@/utils/basePath";

type Props = {
  tag: string;
  tagName: string;
  size?: "sm" | "lg";
};

const { tag, tagName, size = "lg" } = Astro.props;
---

<li>
  <a
    href={withBase(`/tags/${tag}/`)}
    transition:name={tag}
    class:list={[
      "flex items-center gap-1 rounded-sm border border-border bg-accent-quiet px-3 py-1.5 font-ui text-accent no-underline shadow-[0_1px_0_var(--border)]",
      "hover:border-accent hover:text-accent",
      "focus-visible:border-accent focus-visible:text-accent",
      { "text-sm": size === "sm" },
      { "text-lg": size === "lg" },
    ]}
  >
    <IconHash
      class:list={[
        "opacity-80",
        { "size-5": size === "lg" },
        { "size-4": size === "sm" },
      ]}
    />
    {tagName}
  </a>
</li>
